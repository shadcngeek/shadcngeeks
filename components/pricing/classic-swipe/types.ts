import React from "react";

export interface PriceProTypes {
  plan: string;
  title: string;
  price: string;
  className?: string;
  buttonName: string;
  description: string;
  priceClassName?: string;
  buttonClassName?: string;
}
